-- ============================================================
-- profiles 테이블에 Pro 구독 만료일 컬럼 추가
-- 결제 시스템 연동 시 이 컬럼에 만료일을 저장
-- NULL = 만료일 없음 (무기한 또는 수동 관리)
-- ============================================================

ALTER TABLE profiles
  ADD COLUMN IF NOT EXISTS pro_expires_at timestamptz;
